<Page x:Class="Calculadora_IMC.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:Calculadora_IMC.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainPageViewModel}"
      mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/ButtonStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Center"
            Orientation="Vertical"
            Margin="10">
            <TextBlock Text="Usuários"
				   FontSize="30"
				   TextAlignment="Center"
				   Margin="0,10,0,20"/>

            <Button Content="Adicionar Usuário"
            Width="120"
            Height="40"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
			Margin="0,0,5,10"
			Cursor="Hand"
			Style="{StaticResource RoundedBlueButton}"
			Command="{Binding AddUserCommand}"/>

            <TextBox x:Name="SearchBox"
            Width="300"
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
            VerticalContentAlignment="Center"
            Padding="5"/>

            <DataGrid ItemsSource="{Binding UsuariosView}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            Width="625"
            Height="350"
            Margin="10">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="250"/>
                    <DataGridTextColumn Header="Idade" Binding="{Binding Idade}" Width="40"/>
                    <DataGridTextColumn Header="Altura (m)" Binding="{Binding Altura}" Width="70"/>
                    <DataGridTextColumn Header="Peso (kg)" Binding="{Binding PesoUltimaMedicao}" Width="70"/>
                    <DataGridTextColumn Header="Gênero" Binding="{Binding Genero}" Width="60"/>
                    <DataGridTextColumn Header="IMC" Binding="{Binding IMCUltimaMedicao, StringFormat=N2}" Width="35"/>
                    <DataGridTemplateColumn Header="Ações" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="&#xE890;"
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="16"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Padding="0"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.ViewUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Visualizar usuário"/>

                                    <Button Content="&#xE74D;"
                                    FontFamily="Segoe MDL2 Assets"
                                    Foreground="Red"
                                    FontSize="16"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Padding="0"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Deletar usuário"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>